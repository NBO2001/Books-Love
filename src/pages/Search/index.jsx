import React from "react";
import {Navbar } from "../../components";

const Search = ({title}) => {

    return(
        <Navbar title={title}>
            <div>Search</div>
        </Navbar>
    );
};

export default Search;